package kr.co.gitgardner.graphql;

import org.springframework.stereotype.Controller;

// 깃허브 OAuth 이메일을 사용하므로 더 이상 필요하지 않음
// UserGraphQL의 user 쿼리에서 이메일 정보를 제공함
@Controller
public class EmailGraphQL {
    // 이 클래스는 더 이상 사용되지 않지만, 
    // 기존 기능과의 호환성을 위해 유지 (필요시 삭제 가능)
}